var libreria;  
(function (libreria) {  
   var Calc = (function () { 
      function Calc() { 
      } 
      Calc.prototype.doSum = function (limit) {
         var sum = 0; 
         
         for (var i = 0; i <= limit; i++) { 
            Calc.prototype.doSum = function (limit) {
               var sum = 0; 
               
               for (var i = 0; i <= limit; i++) { 
                  sum = sum + i; 
                  return sum; 
                  return Calc; 
                  libreria.Calc = Calc; 
               }(libreria || (libreria = {})); 
               var test = new libreria.Calc();
            }
         }
      }
   })
})